using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class ObjectCreator : EditorWindow
{
    /*
    private GameObject obj;
    private bool isDisplaced;

    [MenuItem("Windows/ObjectCreator")]

    public static void ShowWindow()
    {
        GetWindow<ObjectCreator>("ObjectCreator");
    }

    private void OnGUI()
    {
        GUILayout.Label("GameObject de l'objet \n", EditorStyles.boldLabel);

        EditorGUILayout.BeginHorizontal();
        obj = (GameObject)EditorGUILayout.ObjectField(obj, typeof(GameObject), true);
        EditorGUILayout.EndHorizontal();

        isDisplaced = EditorGUILayout.Toggle("Object is displaced", isDisplaced);

        if (GUILayout.Button("Créer la data d'objet"))
        {
            this.CreateObject();
        }
    }

    private void CreateObject()
    {
        Debug.Log("Creation");
    }
    */
}
